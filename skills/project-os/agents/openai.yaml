interface:
  display_name: "Project OS"
  short_description: "Bootstrap and maintain repository work state"
  default_prompt: "Use $project-os to inspect this repository without changing files and recommend the correct setup."
policy:
  allow_implicit_invocation: false

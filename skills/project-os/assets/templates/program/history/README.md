# History

History contains closed Program contracts and explicitly migrated snapshots. It is not a startup reading list and never overrides current authority, CONTEXT, STATE or active plans.

Every snapshot is listed in `index.json` with its repository-relative path and SHA-256 hash. The checker detects missing or changed indexed content. This makes the archive tamper-evident; it does not make storage immutable.

# Engineering failure knowledge

- `project/failures.json`: confirmed lessons that still depend on this repository or environment.
- `shared/failures.json`: sanitized lessons that can be reused across projects.

Candidates and hypotheses belong in the findings register. Add a knowledge entry only after confirming
the mechanism. Deduplicate by stable ID and retire obsolete advice with its replacement and reason.
